# Database migrations, seeders and factories

This directory will house the files needed for database management and migrations, as well as a database.sqlite, should the user choose to use sqlite as a database driver
